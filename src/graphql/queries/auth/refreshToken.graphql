query refreshToken($refreshToken: String!) {
  refreshToken(refreshToken: $refreshToken) {
    refresh_token
    access_token
    user {
      id
      username
      firstname
      lastname
      age
      rate
      email
      localization
      telNumber
      gender
      isConfirmed
    }
  }
}
